/**
 * TypeScript types for Email Gateway Admin UI
 * 
 * This file re-exports shared types and defines frontend-specific types.
 * All core types are now defined in the shared-types package to ensure
 * consistency between frontend and backend.
 */

// Re-export shared types from the workspace package
// Using relative path to the correct shared-types location
export * from './shared';

